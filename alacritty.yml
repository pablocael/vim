font:
  normal:
    family: "Hack Nerd Font"
  size: 16

# Window title
window:
  title: Vim

env:
  TERM: xterm-256color

shell:
  program: /bin/bash
  args:
    - -l
    - -c
    - "tmux attach || tmux -2"